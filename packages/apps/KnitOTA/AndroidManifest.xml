<?xml version="1.0" encoding="utf-8"?>
<!-- KnitOS OTA Updater - Android Manifest -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.knitos.ota"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REBOOT" />
    <uses-permission android:name="android.permission.RECOVERY" />
    <uses-permission android:name="android.permission.WRITE_SYSTEM_PROPERTIES" />
    
    <application
        android:label="@string/app_name"
        android:icon="@drawable/ic_knit_logo"
        android:theme="@style/Theme.KnitOS"
        android:sharedUserId="android.uid.system">
        
        <!-- Main OTA Activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        
        <!-- Settings entry point -->
        <activity
            android:name=".KnitOTASettingsActivity"
            android:label="@string/update_settings"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.UPDATE_SETTING" />
            </intent-filter>
        </activity>
        
        <!-- Background update service -->
        <service
            android:name=".UpdateCheckService"
            android:exported="false"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED" />
            
        <!-- Boot receiver for scheduled checks -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        
    </application>
</manifest>
